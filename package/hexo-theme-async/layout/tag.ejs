<%- partial(theme.layout.main,  {
    banner: partial(theme.layout.banner,  {
        ...theme.banner.default,
        bannerTitle: page.title,
        ...theme.banner.archive 
    }),
    content: is_tag() ? partial(theme.layout.page_archive) : partial(theme.layout.page_tag)
}) %>